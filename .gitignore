# Design reference / scratch screenshots (large, not part of the live site)
reference/

# Local dev tooling & editor config
.claude/
.vscode/

# Raw source recordings — never committed (the optimised companies-demo.mp4 is what ships)
assets/*.mov
assets/demo-companies.mp4

# macOS
.DS_Store
